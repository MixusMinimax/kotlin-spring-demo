## Compose Setup for Local Building

This folder is identical to [../deploy](../deploy) with the exception that the [compose.yml](compose.yml) file builds
the docker image based on [../Dockerfile](../Dockerfile) in the local project.

For more information, read [../deploy/README.md](../deploy/README.md).
